name: Build & Release APK (signed)

# Se dispara al subir una etiqueta de versión, ej: git tag v1.0.0 && git push origin v1.0.0
on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar el código del repositorio
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Configurar Android SDK
        uses: android-actions/setup-android@v3

      - name: Configurar Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.7

      - name: Dar permisos a gradlew
        run: chmod +x gradlew

      - name: Verificar si hay secretos de firma configurados
        id: check_secrets
        run: echo "has_keystore=${{ secrets.KEYSTORE_BASE64 != '' }}" >> "$GITHUB_OUTPUT"

      - name: Decodificar el keystore desde el secreto
        if: steps.check_secrets.outputs.has_keystore == 'true'
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > release.jks
          cat > keystore.properties << EOF
          storeFile=release.jks
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          EOF

      - name: Compilar APK release
        run: ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Advertencia si no se firmó
        if: steps.check_secrets.outputs.has_keystore != 'true'
        run: |
          echo "::warning::No se configuraron los secretos KEYSTORE_BASE64/KEYSTORE_PASSWORD/KEY_ALIAS/KEY_PASSWORD."
          echo "::warning::El APK generado NO está firmado y no se podrá instalar hasta firmarlo manualmente."

      - name: Subir el APK como artefacto descargable
        uses: actions/upload-artifact@v4
        with:
          name: JetGo-release
          path: app/build/outputs/apk/release/app-release*.apk
          if-no-files-found: error

      - name: Publicar como GitHub Release
        if: steps.check_secrets.outputs.has_keystore == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/release/app-release.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
